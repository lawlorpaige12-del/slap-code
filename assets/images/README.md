# Image Assets

This folder is reserved for visual assets used by the slAP application.

Place your own images here and import them from `assets/images` in the app.

Example:
```ts
import studyIcon from '../assets/images/study-icon.svg';
```

No generated images are included by default.
